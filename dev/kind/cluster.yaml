# Personal agent-lab cluster.
# No port 80/443 mapping — this lab uses kubectl port-forward for host access
# so it can coexist with iden2-dev (which owns 80/443 on the host).
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    image: kindest/node:v1.35.0@sha256:452d707d4862f52530247495d180205e029056831160e22870e37e3f6c1ac31f
